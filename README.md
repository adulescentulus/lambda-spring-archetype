# Maven archetype for creating a Spring Boot app on AWS Lambda

## Quick Start
Until there is an official release in a public repo:
1. Clone project
1. Install the Maven archetype `mvn install`
1. Change to a new directory and create a new project `mvn archetype:generate -DarchetypeCatalog=local`
1. check the created README.md for further instructions
